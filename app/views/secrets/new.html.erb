<div class="userNew">
	<p class="userNewHeader"><%= @title %></p>
	<%= form_for @secret do |f| %>
		<div class="userNewForm">
			<%= render 'shared/error_messages', object: f.object %>
			<div class="field">
				<p>Name</p>
				<%= f.text_field :name, autocomplete: "off" %>
			</div>
			<div class="field">
				<p>Code</p>
				<%= f.text_field :code, autocomplete: 'off' %>
			</div>
			<div class="action">
				<%= f.submit "Save" %>
				<%= link_to "Cancel", secrets_path %>
			</div>
		</div>
	<% end %>
	<div class="clear"></div>
</div>